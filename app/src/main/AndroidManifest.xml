<?xml version="1.0" encoding="utf-8"?>
<manifest
    xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.neurozen.marsroverphotos">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:logo="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:name="com.neurozen.marsroverphotos.application.SampleApplication"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.neurozen.marsroverphotos.activity.MainActivity"
            android:windowSoftInputMode="stateUnchanged|adjustResize"
            >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name="com.neurozen.marsroverphotos.activity.PhotoListActivity"
            android:windowSoftInputMode="stateUnchanged|adjustResize"
            android:parentActivityName="com.neurozen.marsroverphotos.activity.MainActivity" />

    </application>

</manifest>